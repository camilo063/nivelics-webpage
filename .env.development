# Modo fallback — `npm run dev` no conecta a Neon
# Las páginas rendean con valores degradados (nav/home muestran defaults).
# Para conectar a Neon en dev, usar: `npm run dev:db`
USE_DB_FALLBACKS=true
