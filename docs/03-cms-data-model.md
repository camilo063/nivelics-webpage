# 03 — CMS and data model

## The fallback pattern

Every public content query follows the same shape:

```
try DB → if empty, use hardcoded FALLBACK → always return data
```

Concretely, `getAllProductos()` in [lib/cms/productos.ts](../lib/cms/productos.ts)
is the canonical example. If the DB is down or the `productos` table is empty,
the 3 products still render from a hardcoded `FALLBACK` array with the same
shape as the DB row.

**Why this matters**: the site stays alive if Neon goes down, if a seed script
failed, or if an admin accidentally deletes a row. Do not remove fallbacks when
refactoring.

## Database

- **Engine**: PostgreSQL on Neon (serverless)
- **ORM**: Drizzle 0.45
- **Schema**: [lib/db/schema/admin.ts](../lib/db/schema/admin.ts) — a single file with every table
- **Migrations**: [drizzle/migrations/](../drizzle/migrations/) — generated by `drizzle-kit`
- **DB client**: [lib/db/index.ts](../lib/db/index.ts) exports `db` (null when `DATABASE_URL` is missing — every query must handle that)

## Tables

| Table                | Purpose                      | Notes                                                                                          |
| -------------------- | ---------------------------- | ---------------------------------------------------------------------------------------------- |
| `admin_users`        | CMS logins                   | Session auth via `getAdminSession()`                                                           |
| `admin_activity_log` | Audit trail                  | Every admin mutation writes a row                                                              |
| `site_config`        | Global site settings         | `id = 'main'` single row; holds logo, socials, `llmsTxtContent`/`llmsFullTxtContent` overrides |
| `nav_config`         | Mega-menu structure          | JSON payload consumed by `components/layout/nav.tsx`                                           |
| `home_content`       | Home page copy               | Hero, metrics, CTA, FAQs — all bilingual                                                       |
| `servicios`          | Service hub + sub-services   | `service_type` = 'hub' or 'sub'; has CTA, FAQs, features, schema JSON-LD fragment              |
| `industrias`         | Industry verticals           | Hero, pain points, solutions, CTAs — 6 rows                                                    |
| `productos`          | SaaS products                | PAYWL / Niveleads / Hirely. Has features, FAQs, comparison table, pricing                      |
| `casos_exito`        | Client case studies          | 7 rows (Televisa, etc.). Metrics, testimonial, gallery                                         |
| `team_members`       | Leadership team              | Shown on `/nosotros/equipo` with `Person` schema                                               |
| `historia_items`     | Timeline entries             | `/nosotros/historia`                                                                           |
| `certificaciones`    | Credentials / certifications | `/nosotros/certificaciones`                                                                    |
| `pages_general`      | Ad-hoc pages                 | `/contacto`, `/privacidad`, `/soporte`, `/trabaja-con-nosotros` copy                           |
| `blog_posts`         | Blog content                 | Categorized, slugged, tagged                                                                   |
| `blog_categories`    | Blog taxonomy                |                                                                                                |
| `landing_pages`      | `/lp/[slug]` landings        | Block-based schema; `noindex` flag respected by `robots.ts`                                    |
| `media`              | Uploaded media               | Tracks files in `/uploads/` or S3                                                              |
| `leads`              | Captured leads               | Form submissions from `/contacto` and landings                                                 |

> Every bilingual table has `*_es`/`*_en` column pairs for titles, descriptions,
> CTAs, SEO metadata.
> `status` is `contentStatusEnum` (`draft` | `published` | `archived`) on
> content tables.
> `translation_status_en` is `translationStatusEnum` (`pending` | `in_progress`
> | `complete`) — drives the admin "translation queue" UI.

## Read layer (public)

Read queries live in [lib/cms/queries.ts](../lib/cms/queries.ts) and
[lib/cms/productos.ts](../lib/cms/productos.ts). They are **plain async
functions**, not Server Actions.

- `getSiteConfigPublic()` is wrapped with React `cache()` for request-level
  dedup. Marketing layout + sitemap + llms routes can call it on the same
  request and only hit Postgres once.
- `getAllProductos()` has a built-in `FALLBACK` array inside the same file.
- `mapProducto(data, locale)` / `mapIndustria(...)` / `mapHomeContent(...)` etc.
  live in [lib/cms/mappers.ts](../lib/cms/mappers.ts) — they resolve the
  bilingual ES/EN fields against the current locale.

## Write layer (admin)

Mutations are **Server Actions** in `lib/admin/actions/*.actions.ts`:

- [config.actions.ts](../lib/admin/actions/config.actions.ts)
- [productos.actions.ts](../lib/admin/actions/productos.actions.ts)
- industrias, servicios, casos, equipo, historia, certificaciones, landings,
  home, nav, media, usuarios, blog, revalidate

Every Server Action:

1. Calls `getAdminSession()` and throws if missing
2. Performs the DB mutation
3. Writes an `admin_activity_log` row
4. Calls `revalidatePath(...)` for every public path that consumes the data

**Do not call admin Server Actions from marketing pages.** Use the read-only
`getSiteConfigPublic` (not `getSiteConfig` from the admin actions file).

## Translation flow

The admin CMS has a "translation queue" that lists rows with
`translation_status_en != 'complete'`. Admins can:

1. Edit EN fields by hand in the admin form
2. Or call `/api/admin/translate` which uses the OpenAI/Anthropic bridge
   (see [scripts/translate-all.ts](../scripts/translate-all.ts) for the
   headless version)

After translating, admins flip `translation_status_en` to `complete` and the
row stops showing in the queue.

## Content status + noindex

- Every content row has `status`. `published` rows show on the public site;
  `draft` and `archived` do not.
- Landing pages additionally have a `noindex` boolean. When true, the slug
  is added to `robots.txt`'s `Disallow` rules via
  [app/robots.ts](../app/robots.ts).

## Blog content format (HTML)

`blog_posts.content_es` / `content_en` store **HTML** (produced by the Tiptap
WYSIWYG editor in the admin). Rendering on the public site is handled by
[`components/shared/ProseContent.tsx`](../components/shared/ProseContent.tsx),
which:

- Accepts either HTML or legacy markdown and converts markdown via `marked`
  on the fly (server-side). This keeps backwards-compat while the DB migrates
  organically through edits.
- Wraps the content in Tailwind Typography classes (`@tailwindcss/typography`)
  themed for the Nivelics dark-first design system.

Admin edit flow uses [`components/admin/ui/TiptapEditor.tsx`](../components/admin/ui/TiptapEditor.tsx)
(React + Tiptap v3 with StarterKit, Underline, Link, Image, Table, Placeholder,
TextAlign extensions). The editor auto-parses markdown on initial load so
previously-generated markdown posts show formatted, and every save writes
canonical HTML.

Schema.org `BlogPosting` is emitted per-post via
[`lib/schema/blog-posting.ts`](../lib/schema/blog-posting.ts) from the detail
route. `generateMetadata` covers OpenGraph (type=article), Twitter card, and
hreflang alternates (ES/EN/x-default).
